
# Graphify generated output (local analysis only)
graphify-out/

# Agent skills / config (local tooling, not site content)
.agents/
.claude/
skills-lock.json

# Generated deployable client builds (see scripts/build-mm.py)
dist/

# wrangler's local scratch cache. Rewritten on every deploy, and it
# stores the Cloudflare account name — a client's personal email address.
.wrangler/
__pycache__/
